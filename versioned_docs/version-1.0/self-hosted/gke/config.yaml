email:

license:

subdomain: 

auth:
  google:
    enabled: true
    clientId: 
    clientSecret: 

cloud:
  provider:
    gcp:
      enabled: true
      project: 
      bucket:

buildkit:
  ingress:
    enabled: false
  service:
    type: LoadBalancer