email: 

license: 

subdomain: 

auth:
  github:
    enabled: true
    clientId: 
    clientSecret:

cluster:
  endpoint:

registry:
  storage:
    filesystem:
      enabled: true

cloud:
  provider:
    digitalocean:
      enabled: true

buildkit:
  ingress:
    enabled: false
  service:
    type: LoadBalancer