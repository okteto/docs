email: 

license: 

subdomain: 

auth:
  github:
    enabled: true
    clientId: 
    clientSecret:
    organization:

cluster:
  endpoint:

registry:
  storage:
    provider:
      azure:
        enabled: true
        storage:
          container: ""
          accountName: ""

buildkit:
  ingress:
    enabled: false
  service:
    type: LoadBalancer
  persistence:
    enabled: true
