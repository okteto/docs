email: 

license: 

subdomain: 

auth:
  github:
    enabled: true
    clientId: 
    clientSecret:

cloud:
  provider:
    civo:
      enabled: true

registry:
  storage:
    filesystem:
      enabled: true
      persistence:
        size: 100Gi