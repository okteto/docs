name: docs

build:
  docs:
    context: .
  dev:
    context: .
    target: build

deploy:
  compose: docker-compose.yaml

dev:
  docs:
    image: ${OKTETO_BUILD_DEV_IMAGE}
    sync:
      - .:/app
    workdir: /app
    command: "yarn start"