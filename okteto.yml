name: docs

build:
  docs:
    context: .

deploy:
  compose: docker-compose.yml

dev:
  docs:
    image: ${OKTETO_BUILD_DOCS_IMAGE}
    sync:
      - .:/app
    workdir: /app
    command: "yarn start"
    forward:
      - 8080:8080
